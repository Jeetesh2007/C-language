#include<stdio.h>
int main()
{ float a;
  float area;
  float area_of_cube;
 printf("enter length of side of square");
 scanf("%f",&a);
 area= a*a;
 printf("area of square is%f",area);
 area_of_cube=6*(a*a);
 printf("area of cube is %f\n",area_of_cube);
 
 }
