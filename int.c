#include <stdio.h>
int main ()
{ float num;
  printf("enter your number");
  scanf("%f",&num);
  if(num > 0)
    printf("num is postive");
  if(num == 0)
    printf("num is zero");
  if(num < 0)
    printf("num is negative");
  }
